<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ligg.common.mapper.order.OrderMapper">

    <!-- 查询订单信息 -->
    <select id="selectOrderByOrderId" resultType="com.ligg.common.module.dto.OrderInfoDto">
        select o.id,
               o.order_no,
               o.user_id,
               o.total_amount,
               o.pay_type,
               o.address_id,
               o.remark,
               o.status,
               o.create_time,
               oi.quantity,
               oi.product_id
        from orders o
                 left join order_item oi on o.id = oi.order_id
        where o.order_no = #{orderId}
    </select>
</mapper>
