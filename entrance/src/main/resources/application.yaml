server:
  port: 8899
spring:
  application:
    name: Ecommerce
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/e_commerce?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 200309
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true # 开启自动驼峰命名
jwt:
  key: ligg

token:
  expire: 6 #过期时间

##文件配置
file:
  image:
    base-path: resources/image # 用相对路径(相对于项目运行目录)
    relative-path: /image # 图片访问的相对路径前缀(用于返回给前端)
