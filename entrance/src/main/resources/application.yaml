server:
  port: 8899
spring:
  application:
    name: E-commerce-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/e_commerce?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 200309
  data:
    ## redis配置
    redis:
      host: 127.0.0.1
      port: 6379
      username:
      password:
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  ## 上传文件配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true # 开启自动驼峰命名
  mapper-locations: classpath*:/mapper/**/*.xml

jwt:
  key: ligg #加密密钥

##文件配置
file:
  image:
    base-path: resources/image # 用相对路径(相对于项目运行目录)
    relative-path: /api/image # 图片访问的相对路径前缀(用于返回给前端)

api:
  base-url: "http://localhost:8899" #填写后端的ip或域名(用于拼接完整的资源访问路径)
